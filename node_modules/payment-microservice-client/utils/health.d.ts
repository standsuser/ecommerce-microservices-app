import { Server } from 'socket.io';
import { Request, Response } from 'express';
export declare const initAliveInterval: () => void;
export declare const emitInTime: (server: Server) => void;
export declare const onAlive: () => void;
export declare const healthCheck: (_: Request, res: Response) => void;
