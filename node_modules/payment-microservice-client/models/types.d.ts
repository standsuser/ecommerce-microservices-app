/// <reference types="node" />
import * as http from 'http';
import { Express } from 'express';
import { Server, Socket } from 'socket.io';
export interface SetupProps {
    httpServer?: http.Server;
    expressApp?: Express;
    socketServer?: Server;
    httpPort: number;
    redisPort?: number;
    socketPath?: string;
    redisHost: string;
    clientName: string;
    healthEndpoint?: string;
    connectFn?: (socket: Socket) => void;
    key?: string;
    logLevel?: string;
}
export interface PaymentServer {
    id: string;
    name: string;
    time: Date;
}
export interface SocketMessage<ReturnType> {
    ok: boolean;
    e?: string;
    stack?: string;
    d: ReturnType;
}
export interface APIFunctionInterfaceRedis<ReturnType> {
    (hostname: string, ...params: (string | Record<string, unknown>)[]): Promise<ReturnType>;
}
